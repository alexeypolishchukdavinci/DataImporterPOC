#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../hot-builder/bin/hot-builder" "$@"
  ret=$?
else 
  node  "$basedir/../hot-builder/bin/hot-builder" "$@"
  ret=$?
fi
exit $ret
